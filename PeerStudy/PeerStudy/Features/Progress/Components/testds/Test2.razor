@*@page "/test2"
@using Blazorise;

<BarChart @ref="barChart" Data="datasets"></BarChart>


@code {
    private BarChart<int> barChart;
    private List<ChartDataset<int>> datasets;
    private List<string> labels;

    protected override void OnInitialized()
    {
        datasets = new List<ChartDataset<int>>();
        labels = new List<string> { "Student 1", "Student 2", "Student 3" };

        // Assuming you have a list of assignments
        List<string> assignments = new List<string> { "Assignment 1", "Assignment 2", "Assignment 3" };

        // Iterate over each assignment
        foreach (string assignment in assignments)
        {
            ChartDataset<int> dataset = new ChartDataset<int>
                {
                    
                    Label = assignment,
                    Data = new List<int> { 80, 85, 90 } // Points for each student in this assignment
                                                        // Add more data points for each student in this assignment
                };

            datasets.Add(dataset);
        }
    }

}*@@*@page "/test2"
@using Blazorise;

<BarChart @ref="barChart" Data="datasets"></BarChart>


@code {
    private BarChart<int> barChart;
    private List<ChartDataset<int>> datasets;
    private List<string> labels;

    protected override void OnInitialized()
    {
        datasets = new List<ChartDataset<int>>();
        labels = new List<string> { "Student 1", "Student 2", "Student 3" };

        // Assuming you have a list of assignments
        List<string> assignments = new List<string> { "Assignment 1", "Assignment 2", "Assignment 3" };

        // Iterate over each assignment
        foreach (string assignment in assignments)
        {
            ChartDataset<int> dataset = new ChartDataset<int>
                {
                    
                    Label = assignment,
                    Data = new List<int> { 80, 85, 90 } // Points for each student in this assignment
                                                        // Add more data points for each student in this assignment
                };

            datasets.Add(dataset);
        }
    }

}*@