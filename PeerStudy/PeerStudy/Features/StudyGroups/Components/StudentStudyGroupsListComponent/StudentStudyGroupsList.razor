@using PeerStudy.Core.Models.StudyGroups;
@using PeerStudy.Features.StudyGroups.Components.StudentStudyGroupCardComponent;
@using PeerStudy.Features.StudyGroups.Components;

@inherits StudyGroupsListBase<StudentStudyGroupDetailsModel>

<div class="study-groups-container center-elements">
    @if (IsLoading)
    {
        <div>
            <MatProgressCircle Indeterminate="@IsLoading" Size="MatProgressCircleSize.Large" />
        </div>
    }
    else if (!IsLoading && !StudyGroups.Any())
    {
        <NotFound Message="There are no study groups yet..."></NotFound>
    }
    else
    {
        <div class="study-groups">
            @foreach (var group in StudyGroups)
            {
                <div>
                    <StudentStudyGroupCard StudyGroupModel="group"
                            OnClicked="HandleClick">
                    </StudentStudyGroupCard>
                </div>
            }
        </div>
    }
</div>